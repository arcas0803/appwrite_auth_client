name: appwrite_auth_client
description: "A new Flutter package project."
version: 0.0.1
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
homepage: null

environment:
  sdk: '>=3.2.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  appwrite: ^11.0.1
  common_classes:
    git:
      url: https://github.com/arcas0803/common_classes.git
      ref: main
  connectivity_client:
    git:
      url: https://github.com/arcas0803/connectivity_client.git
      ref: main
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.1
  logger: ^2.0.2+1
  uuid: ^4.3.3

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: null
